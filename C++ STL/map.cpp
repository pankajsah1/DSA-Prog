#include<iostream>
using namespace std;
void explainMap(){
    map<int, int> mpp;       //  map => map<key, value> mapp => map always sotre unique keys and in sorted order

}
int main(){
    explainMap();
    return 0;
}
